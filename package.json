{
  "name": "org-iso-8583",
  "private": true,
  "description": "ISO 8583 bitmaps, BCD/ASCII field codec, and message encode/decode in portable .cljc",
  "scripts": {
    "test:cljs": "nbb --classpath \"$(clojure -A:cljs -Spath)\" scripts/verify-cljs.cljs"
  }
}
